# TOVCH (VYRU5) — PROMPT для AI-отладки (Grok)

**Дата создания:** 2026-02-27  
**Версия проекта:** v30  
**Статус:** Рабочая версия с известными проблемами

---

## **КОНТЕКСТ ЗАДАЧИ**

Ты — AI-разработчик (Grok), получивший доступ к полному исходному коду многопользовательской браузерной игры "TOVCH" (вирусная битва). Твоя задача — проанализировать код, найти и исправить баги, связанные с визуализацией битвы вирусов.

---

## **ОПИСАНИЕ ПРОБЛЕМЫ**

### **Симптомы:**

1. **Клетки вирусов не отображаются на canvas**
   - Логи показывают распространение: `RED=1, BLUE=1 → RED=2, BLUE=2`
   - Прогресс-бар в верхней панели заполняется correctly
   - **НО:** На canvas не видно красных/синих клеток

2. **Ошибки в консоли:**
   ```
   [BattleRenderer] Grid size mismatch: 640 != 2304
   ```

3. **Предупреждения PixiJS v8:**
   ```
   Graphics#beginFill is no longer needed
   Graphics#drawCircle has been renamed to Graphics#circle
   Graphics#lineStyle is no longer needed
   ```

### **Шаги воспроизведения:**

1. Открыть https://cursor-hold-game.onrender.com
2. Создать комнату (Create Room)
3. Открыть вторую вкладку, присоединиться (Join Room + ID)
4. Оба игрока распределяют 12 очков по параметрам вируса
5. Оба нажимают READY
6. Появляется обратный отсчёт 3-2-1
7. Нажать кнопку СТАРТ
8. **Ожидание:** Видно красные клетки (сверху) и синие (снизу)
9. **Реальность:** Canvas пустой, клетки не видны

---

## **ТРЕБОВАНИЯ К ИСПРАВЛЕНИЮ**

### **1. Критические (блокирующие):**

- ✅ **BattleRenderer должен отображать клетки вирусов**
  - Красные (0xff3333) для Player 1
  - Синие (0x3333ff) для Player 2
  - Размер клеток: 25px диаметр, 1px зазор
  - Свечение: 0.8 alpha, +5px радиус

- ✅ **Инициализация BattleRenderer должна происходить при старте битвы**
  - НЕ в конструкторе MainApp
  - В `networkManager.onVirusBattleStarted()`
  - С правильными размерами: `initGrid(20, 32)`

- ✅ **Сетка должна быть 20×32 = 640 клеток**
  - НЕ 64×36 = 2304
  - Server отправляет: `width: 20, height: 32`
  - Client должен инициализировать: `battleRenderer.initGrid(20, 32)`

### **2. Важные (функциональные):**

- ⚠️ **Обновление клеток при каждом тике (500ms)**
  - `BattleRenderer.updateGrid()` должен вызываться
  - Клетки должны менять цвет (empty → virusA/virusB)

- ⚠️ **z-index BattleRenderer = 1000**
  - Сетка: z-index = 0 (задний план)
  - Вирусы: z-index = 1000 (передний план)

### **3. Желательные (косметические):**

- 📝 **Убрать предупреждения PixiJS v8**
  - Заменить `beginFill()` → `fill()`
  - Заменить `drawCircle()` → `circle()`
  - Заменить `lineStyle()` → `setStrokeStyle()`

---

## **СТРУКТУРА ФАЙЛОВ В ЭТОЙ ПАПКЕ**

Все файлы находятся в корне DEBUG папки (плоская структура). Имена файлов содержат префиксы, указывающие на их расположение в проекте:

```
DEBUG/
├── package.json.txt                      # Корневой package.json
├── client.package.json.txt               # client/package.json
├── client.src.main.ts.txt                # client/src/main.ts
├── client.src.core.GameEngine.ts.txt     # client/src/core/GameEngine.ts
├── client.src.core.NetworkManager.ts.txt # client/src/core/NetworkManager.ts
├── client.src.features.battle.BattleManager.ts.txt
├── client.src.features.battle.BattleRenderer.ts.txt
├── server.src.rooms.HoldingRoom.ts.txt   # server/src/rooms/HoldingRoom.ts
├── server.src.rooms.schema.ts.txt        # server/src/rooms/schema.ts
├── prompt.txt                            # ЭТОТ ФАЙЛ
└── index.html                            # Навигация по файлам
```

### **Формат каждого файла:**

```txt
========================================
ORIGINAL FILE PATH: C:\__Qwen1\TOVCH\client\src\main.ts
========================================

[полное содержимое файла...]
```

---

## **КЛЮЧЕВЫЕ ФАЙЛЫ ДЛЯ АНАЛИЗА**

Начни анализ с этих файлов (в порядке приоритета):

1. **client.src.main.ts.txt** — точка входа, создание BattleRenderer
2. **client.src.features.battle.BattleRenderer.ts.txt** — визуализация клеток
3. **client.src.features.battle.BattleManager.ts.txt** — логика битвы
4. **server.src.rooms.HoldingRoom.ts.txt** — серверная логика
5. **client.src.core.GameEngine.ts.txt** — инициализация PixiJS

---

## **АЛГОРИТМ ОТЛАДКИ**

### **Шаг 1: Проверь создание BattleRenderer**

Открой `client.src.main.ts.txt`, найди:
```typescript
// ❌ НЕПРАВИЛЬНО (в конструкторе):
this.battleRenderer = new BattleRenderer(this.gameEngine.app!.stage);

// ✅ ПРАВИЛЬНО (при старте битвы):
this.networkManager.onVirusBattleStarted = (data) => {
  if (!this.battleRenderer && this.gameEngine.app) {
    this.battleRenderer = new BattleRenderer(this.gameEngine.app.stage);
    this.battleRenderer.initGrid(data.width, data.height); // 20×32
  }
};
```

### **Шаг 2: Проверь размеры сетки**

Открой `client.src.features.battle.BattleRenderer.ts.txt`, найди:
```typescript
initGrid(width: number, height: number): void {
  this.gridWidth = width;   // Должно быть 20
  this.gridHeight = height; // Должно быть 32
  this.totalCells = width * height; // 640
  // ...
}
```

### **Шаг 3: Проверь updateGrid**

Убедись, что `updateGrid` вызывается при каждом тике:
```typescript
// В BattleManager.ts
private spreadTick(): void {
  // ... логика распространения ...
  
  // Обновляем сетку
  this.gridData.grid = newGrid;
  
  // Вызываем callback
  if (this.onGridUpdateCallback) {
    this.onGridUpdateCallback(this.gridData.grid);
  }
}
```

### **Шаг 4: Запусти локально**

```bash
# Terminal 1: Server
cd server && npm run dev

# Terminal 2: Client
cd client && npm run dev

# Browser: http://localhost:3000
# Open F12 Console
```

### **Шаг 5: Проверь логи**

В консоли браузера должно быть:
```
[BattleRenderer] Init grid: 20×32 = 640 cells
[BattleRenderer] Grid updated: RED=1, BLUE=1, EMPTY=638, TOTAL=640
[BattleRenderer] Grid updated: RED=2, BLUE=2, EMPTY=636, TOTAL=640
[BattleRenderer] Grid updated: RED=3, BLUE=3, EMPTY=634, TOTAL=640
...
```

---

## **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ**

После исправления:

1. **Консоль:**
   ```
   [BattleRenderer] Init grid: 20×32 = 640 cells
   [BattleRenderer] Grid updated: RED=1, BLUE=1, EMPTY=638, TOTAL=640
   [BattleRenderer] Grid updated: RED=2, BLUE=2, EMPTY=636, TOTAL=640
   ```

2. **Canvas:**
   - Видно сетку (cyan, 30% alpha)
   - Видно красную клетку сверху (центр)
   - Видно синюю клетку снизу (центр)
   - Клетки распространяются каждые 500ms

3. **Прогресс-бар:**
   - Красный заполняется слева
   - Синий заполняется справа
   - Обновляется при каждом тике

---

## **КОНТАКТЫ**

- **Репозиторий:** https://github.com/kabuto-lab/cursor-hold-game
- **Client:** https://cursor-hold-game.onrender.com
- **Server:** https://cursor-hold-game-server.onrender.com/health

---

**УДАЧИ В ОТЛАДКЕ! 🚀**
