// BattleRenderer.ts - only pixi-graphics, no logic
import * as PIXI from 'pixi.js';

export class BattleRenderer {
  constructor(app: PIXI.Application) {
    // Placeholder - only pixi graphics
  }
}